#include<bits/stdc++.h>
using namespace std;
#define ll long long
const int mod = 1e9 + 7;
int mxn = 1e5;

int main()
{
    int n, k;
    cin >> n >> k;
    vector<int> x(n), m(n - k + 1);
    for(int i = 0; i < n; i++) cin >> x[i];
    multiset<int> mst1, mst2;
    
}